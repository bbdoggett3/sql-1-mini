-- CREATE TABLE movies (
-- 	movie_id SERIAL,
--   movie_name VARCHAR(30),
--   awesome BOOLEAN
  
-- );

-- INSERT INTO movies (
-- 	movie_name, 
--   awesome
-- ) VALUES (
-- 	'BloodShot',
--   false
-- );

-- SELECT movie_id, movie_name 
-- FROM movies 
-- WHERE awesome = false;

-- UPDATE movies 
-- SET awesome = true
-- WHERE movie_name = 'BloodShot';

-- SELECT * FROM movies WHERE awesome = true;

-- DELETE FROM movies 
-- WHERE movie_name = 'BloodShot';

-- SELECT * FROM movies ORDER BY movie_id ASC;

-- DROP TABLE movies;  This is how you delete the table.

--Mini project

--step 1
-- SELECT * FROM artist;

--step 2
-- SELECT first_name, last_name, country
-- FROM employee;

--step 3
-- SELECT name, composer, milliseconds
-- FROM track
-- WHERE milliseconds > 299000;

--step 4
-- SELECT count(*) 
-- FROM track 
-- WHERE milliseconds > 299000;

--black diamond
--#1
-- SELECT AVG(milliseconds) FROM track;
--#2
-- SELECT sum(total) FROM invoice WHERE billing_country = 'USA';
--#3
-- SELECT first_name FROM customer WHERE first_name LIKE '%a%';
--#4
-- SELECT milliseconds FROM track ORDER BY milliseconds DESC LIMIT 10; 
--#5
-- SELECT milliseconds FROM track ORDER BY milliseconds ASC LIMIT 20;
--#6
-- SELECT first_name, last_name FROM customer WHERE state IN ('CA', 'WA');
--#7
-- SELECT first_name, last_name FROM customer WHERE state IN ('CA', 'WA', 'UT', 'FL', 'AZ');
--#8
-- INSERT INTO artist (
-- 	name
-- ) VALUES(
-- 	'Death Cab For Cutie'
-- );
--#9
-- INSERT INTO customer(
--   support_rep_id,
-- 	first_name,
--   last_name,
--   company,
--   address,
--   city,
--   state,
--   country,
--   postal_code,
--   phone,
--   fax,
--   email
-- ) VALUES(
-- 	3,
--   'Ben',
--   'Doggett',
--   'Devmountain',
--   '175 W. 3rd S.',
--   'Rexburg',
--   'ID',
--   'USA',
--   '83440',
--   '208-356-7322',
--   '208-432-7633',
--   'bdog@gmail.com' 
-- );

-- SELECT * FROM customer WHERE first_name LIKE 'B%' ;
--#10
-- SELECT name FROM playlist WHERE name LIKE 'Classic%';
